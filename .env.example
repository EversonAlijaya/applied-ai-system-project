# Copy this file to a new file named ".env" and paste your real key after the =.
# The real .env file is git-ignored, so your key stays private and is never
# pushed to GitHub. Get a free key at https://aistudio.google.com (Get API key).

GEMINI_API_KEY=your-key-goes-here
